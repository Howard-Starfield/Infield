/** Max width (px) for centered note / workspace document column (was 1200; +25% for editor/toolbar breathing room) */
export const EDITOR_CONTENT_MAX_WIDTH_PX = 1500

/** Horizontal padding (px) for editor scroll region */
export const EDITOR_SCROLL_PADDING_X_PX = 16
